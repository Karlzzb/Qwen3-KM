```flow
    A[原始文本数据] --> B[数据处理/清洗];
    B --> C{是否存在专用词表?};
    C -- 否 --> D[训练Tokenizer];
    C -- 是 --> E[使用现有词表];
    D --> F[预训练<br>海量数据/学习通用知识];
    E --> F;
    F -- 产出 --> G[基座模型<br>如LLaMA, Qwen];
    G --> H[SFT训练<br>指令微调/学习遵循指令];
    H -- 产出 --> I[SFT模型<br>初步的聊天助手];
    I --> J[LoRA训练];
    subgraph J [参数高效微调]
        J1[注入LoRA适配器]
        J2[冻结原模型权重]
    end
    J -- 可选路径1<br>快速适配 --> K[LoRA模型<br>适用于特定任务];
    I --> L[DPO训练];
    subgraph L [基于人类偏好对齐]
        L1[使用偏好数据]
        L2[优化回答质量]
    end
    L -- 可选路径2<br>质量提升 --> M[DPO模型<br>更优质/安全的助手];
    
    K --> N[模型合并/部署];
    M --> N;
```